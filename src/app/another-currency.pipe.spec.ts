import { AnotherCurrencyPipe } from './another-currency.pipe';

describe('AnotherCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new AnotherCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
