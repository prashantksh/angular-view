import { OtherPipePipe } from './other-pipe.pipe';

describe('OtherPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OtherPipePipe();
    expect(pipe).toBeTruthy();
  });
});
