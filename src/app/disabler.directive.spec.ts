import { DisablerDirective } from './disabler.directive';

describe('DisablerDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablerDirective();
    expect(directive).toBeTruthy();
  });
});
